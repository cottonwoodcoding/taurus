import React from 'react';
import { backGround, gradient } from '../styles/main.scss';

const Home = () => (
  <div className={gradient}>
    <div className={backGround}>
    </div>
  </div>
)

export default Home;
